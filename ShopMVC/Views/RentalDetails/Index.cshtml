@model IEnumerable<ShopMVC.Models.RentalDetail>

@{
    ViewData["RentalDetail"] = "RentalDetail";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RentalHeader)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RentalHeader.RentalHeaderId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Movie.Director)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RentalHeaderDetailId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RentalHeaderDetailId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RentalHeaderDetailId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
